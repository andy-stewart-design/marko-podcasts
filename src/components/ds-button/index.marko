class {
    handleClick() {
        console.log("hello!");
    }
}

<button class=`test-button ${input.size}` on-click("handleClick")>
    <${input.renderBody}/>
</button>
style {
    .test-button {
        background-color: blue;
    }
    .small {
        padding: 8px;
    }
    .medium {
        padding: 16px;
    }
    .large {
        padding: 24px;
    }
}
